import { PhoneNnumbersPipe } from './phone-nnumbers.pipe';

describe('PhoneNnumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneNnumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
